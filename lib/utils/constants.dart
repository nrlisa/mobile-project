class AppConstants {
  // REPLACE with your Laptop's IP Address (ipconfig / ifconfig)
  // Do NOT use 'localhost' if testing on Android Emulator
  static const String apiBaseUrl = 'http://192.168.1.XX/exhibition_api'; 
}